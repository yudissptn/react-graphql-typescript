query IdentifyLocker($lockerId: Int!) {
  identifyLocker(lockerId: $lockerId) {
    errors {
      field
      message
    }
    locker {
      id
      lockerId
      address
      lockStatus
      fillStatus
    }
  }
}
